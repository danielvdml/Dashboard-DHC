'use strict';

describe('myApp.profesor module', function() {

  beforeEach(module('myApp.profesor'));

  describe('profesor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});